---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: &name k8s-monitoring-logs
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden-secrets-manager
  target:
    name: *name
    template:
      data:
        username: "{{ .GC_LOGS_USER }}"
        password: "{{ .GC_TOKEN }}"
  dataFrom:
    - extract:
        key: grafana-cloud
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: &name k8s-monitoring-metrics
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden-secrets-manager
  target:
    name: *name
    template:
      data:
        username: "{{ .GC_METRICS_USER }}"
        password: "{{ .GC_TOKEN }}"
  dataFrom:
    - extract:
        key: grafana-cloud
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: &name k8s-monitoring-traces
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden-secrets-manager
  target:
    name: *name
    template:
      data:
        username: "{{ .GC_TRACES_USER }}"
        password: "{{ .GC_TOKEN }}"
  dataFrom:
    - extract:
        key: grafana-cloud
