---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: &name homepage
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: bws-cache
  target:
    name: *name
    template:
      engineVersion: v2
      data:
        HOMEPAGE_VAR_OPNSENSE_USER: "{{ .OPNSENSE_USER }}"
        HOMEPAGE_VAR_OPNSENSE_PASS: "{{ .OPNSENSE_PASS }}"

        HOMEPAGE_VAR_PROXMOX_API_TOKEN_ID: "{{ .PROXMOX_API_TOKEN_ID }}"
        HOMEPAGE_VAR_PROXMOX_API_TOKEN_SECRET: "{{ .PROXMOX_API_TOKEN_SECRET }}"

        HOMEPAGE_VAR_CF_ACCOUNT_ID: "{{ .CF_ACCOUNT_ID }}"
        HOMEPAGE_VAR_CF_TUNNEL_ID: "{{ .CF_TUNNEL_ID }}"
        HOMEPAGE_VAR_CF_API_TOKEN: "{{ .CF_API_TOKEN }}"

        HOMEPAGE_VAR_QNAP_NAME: "{{ .QNAP_NAME }}"
        HOMEPAGE_VAR_QNAP_MODEL: "{{ .QNAP_MODEL }}"
        HOMEPAGE_VAR_QNAP_URL: "{{ .QNAP_URL }}"
        HOMEPAGE_VAR_QNAP_RO_USER: "{{ .QNAP_RO_USER }}"
        HOMEPAGE_VAR_QNAP_RO_PASS: "{{ .QNAP_RO_PASS }}"

        HOMEPAGE_VAR_QNAP_BACKUP_NAME: "{{ .QNAP_BACKUP_NAME }}"
        HOMEPAGE_VAR_QNAP_BACKUP_MODEL: "{{ .QNAP_BACKUP_MODEL }}"
        HOMEPAGE_VAR_QNAP_BACKUP_URL: "{{ .QNAP_BACKUP_URL }}"
        HOMEPAGE_VAR_QNAP_BACKUP_RO_USER: "{{ .QNAP_BACKUP_RO_USER }}"
        HOMEPAGE_VAR_QNAP_BACKUP_RO_PASS: "{{ .QNAP_BACKUP_RO_PASS }}"
        ## Downloads
        HOMEPAGE_VAR_LIDARR_TOKEN: "{{ .LIDARR_API_KEY }}"
        HOMEPAGE_VAR_PROWLARR_TOKEN: "{{ .PROWLARR_API_KEY }}"
        HOMEPAGE_VAR_RADARR_TOKEN: "{{ .RADARR_API_KEY }}"
        HOMEPAGE_VAR_READARR_TOKEN: "{{ .READARR_API_KEY }}"
        HOMEPAGE_VAR_SABNZBD_TOKEN: "{{ .SABNZBD_API_KEY }}"
        HOMEPAGE_VAR_SONARR_TOKEN: "{{ .SONARR_API_KEY }}"
        ## Media
        HOMEPAGE_VAR_PLEX_TOKEN: "{{ .PLEX_TOKEN }}"
  dataFrom:
    - extract:
        key: opnsense
    - extract:
        key: proxmox
    - extract:
        key: cloudflare
    - extract:
        key: qnap
    - extract:
        key: qnap-backup
    - extract:
        key: lidarr
    - extract:
        key: prowlarr
    - extract:
        key: radarr
    - extract:
        key: readarr
    - extract:
        key: sabnzbd
    - extract:
        key: sonarr
    - extract:
        key: plex
